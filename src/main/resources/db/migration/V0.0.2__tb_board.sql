CREATE TABLE wanted_basic.board (
    id                      BIGINT          AUTO_INCREMENT,
    title                   VARCHAR(255),
    content                 VARCHAR(255),
    PRIMARY KEY (id)
);